#include <bits/stdc++.h>
using namespace std;

/*
***********Custom Sort Function*************
bool SortX(Node &a, Node&b) {
    return a.x < b.x;
}
sort(toSort.begin(), toSort.end(), SortX);

struct PT {
  double x, y, z;
  PT() {}
  PT(double X, double Y, double Z) {
    x = X;
    y = Y;
    z = Z;
  }
};
***********3D Euclidean Distance*****************
double calcDist3D(PT &a, PT &b) {
  double xDiff = abs(a.x - b.x), yDiff = abs(a.y - b.y), zDiff = abs(a.z - b.z);
  xDiff = pow(xDiff, 2);
  yDiff = pow(yDiff, 2);
  zDiff = pow(zDiff, 2);
  double ans = sqrt(xDiff + yDiff + zDiff);
  return ans;
}
*/